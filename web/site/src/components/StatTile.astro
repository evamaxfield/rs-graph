---
interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---

<div class="stat-tile">
  <span class="value">{value}</span>
  <span class="label">{label}</span>
</div>
