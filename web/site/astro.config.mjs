import { defineConfig } from 'astro/config';

// GitHub Pages base path -- rs-graph repo served at /rs-graph/
export default defineConfig({
  site: 'https://evamaxfield.github.io',
  base: '/rs-graph',
});
